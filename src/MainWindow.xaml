<Window x:Class="OStimConversionTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:OStimConversionTool"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="SexLab to OStim Animation Conversion Tool"  WindowStyle="None"
        Height="{x:Static SystemParameters.PrimaryScreenHeight}"
        Width="{x:Static SystemParameters.PrimaryScreenWidth}">

    <Window.Resources>
        <local:AnimationDatabase x:Key="animationDatabase" />
        <CollectionViewSource x:Key="cvsAnimationDatabase" Source="{StaticResource animationDatabase}">
        </CollectionViewSource>
    </Window.Resources>

    <DockPanel Margin="10,10,10,10">
        <WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0,0,0,10" RenderTransformOrigin="0.478,7.491" />
        <DataGrid x:Name="animationDatabaseGrid"
          ItemsSource="{Binding Source={StaticResource cvsAnimationDatabase}}"
          CanUserAddRows="False" Visibility="Visible" Width="1440">
        </DataGrid>
        <Button x:Name="Run" Click="ConversionProcess_Click" Content = "Convert" DockPanel.Dock="Bottom" RenderTransformOrigin="0.5,0.5" Height="38" Width="167">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform />
                    <SkewTransform />
                    <RotateTransform />
                    <TranslateTransform X="0" Y="-5" />
                </TransformGroup>
            </Button.RenderTransform>
        </Button>
        <Grid Width="460" RenderTransformOrigin="0.499,-0.241" Height="260">
            <xctk:WatermarkTextBox x:Name="AnimName" Watermark="Animation Name" RenderTransformOrigin="0.558,-17.968" Margin="0,100,0,114" VerticalAlignment="Top" />

            <xctk:WatermarkTextBox x:Name="AnimClass" Watermark="Animation Class" Margin="0,147,0,0" VerticalAlignment="Top" />
            <Button x:Name="ChooseFiles" Click="ChooseFiles_Click" Content="Choose files" RenderTransformOrigin="0.5,0.5" Margin="0,200,0,0" VerticalAlignment="Top" />
        </Grid>
    </DockPanel>
</Window>
